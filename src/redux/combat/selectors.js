export const getInCombat = (state) => state.combat.inCombat;
export const getEnemyStats = (state) => state.combat.enemyStats;
export const getExtraEnemies = (state) => state.combat.extraEnemies;
export const getNextPage = (state) => state.combat.pageAfterCombat;
